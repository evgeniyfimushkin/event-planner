name: build and push auth-service

on:
  push:
    branches:
      - backend

jobs:
#  publish:
#    runs-on: ubuntu-latest
#
#    strategy:
#      matrix:
#        service:
#          - auth-service
#          - event-service
#          - registration-service
#
#    steps:
#      - name: checkout repository
#        uses: actions/checkout@v2
#
#      - name: Build docker image for ${{ matrix.service }}
#        run: |
#          docker build -t evgeniyfimushkin/${{ matrix.service }}:latest services/${{ matrix.service }}
#
#      - name: Log in to DockerHub
#        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin
#
#      - name: Push docker image for ${{ matrix.service }}
#        run: docker push evgeniyfimushkin/${{ matrix.service }}:latest
  deploy:
    #needs: publish
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Test SSH connection
        run: ssh -i <(echo "${{ secrets.SSH_PRIVATE_KEY }}") ${{ secrets.REMOTE_USER }}@${{ secrets.REMOTE_HOST }} "echo 'Connected successfully'"

      - name: Copy docker-compose-yaml
        uses: garygrossgarten/github-action-scp@release
        with:
          local: services/docker-compose.yaml
          remote: ./event-planner/docker-compose.yml
          host: ${{ secrets.REMOTE_HOST }}
          username: ${{ secrets.REMOTE_USER }}
          privateKey: ${{ secrets.SSH_PRIVATE_KEY }}
